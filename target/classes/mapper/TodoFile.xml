<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.oti.project.mapper.TodoFileMapper">

    <insert id="insertTodoFile" parameterType="kr.or.oti.project.domain.TodoFile">
        <selectKey keyProperty="file_id" resultType="Long" order="BEFORE">
            SELECT todo_file_seq.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO todo_file (file_id, todo_id, file_url, file_name)
        VALUES (#{file_id}, #{todo_id}, #{file_url}, #{file_name})
    </insert>

	<select id="selectFileById" parameterType="Long" resultType="kr.or.oti.project.domain.TodoFile">
	    SELECT file_id AS file_id, todo_id AS todo_id, file_url AS file_url, file_name AS file_url
	    FROM todo_file
	    WHERE file_id = #{file_id}
	</select>

    <select id="selectFilesByTodoId" parameterType="Long" resultType="kr.or.oti.project.domain.TodoFile">
        SELECT file_id AS file_id, todo_id AS todo_id, file_url AS file_url, file_name AS file_name
        FROM todo_file
        WHERE todo_id = #{todo_id}
    </select>

    <delete id="deleteFilesByTodoId" parameterType="Long">
        DELETE FROM todo_file
        WHERE todo_id = #{todo_id}
    </delete>

    <delete id="deleteTodoFileById" parameterType="Long">
        DELETE FROM todo_file
        WHERE file_id = #{file_id}
    </delete>

</mapper>